description: Unauthorized
content:
  application/json:
    schema:
      type: object
      properties:
        Error:
          type: string
          example: Authentication required to access this resource